Make sure you have yt-dlp installed and runnable at the jar file directory.

You can config proxy rules with regex: ^(.*\.)?(cloudfront|ttvnw|jtvnw)\.net$ to direct the twitch cnd traffic to save the proxy traffic

The default video download duration is based on the rta of the run and additionally plused 20 seconds. Program can download the recent 7 days(within a week) undownloaded runs.
